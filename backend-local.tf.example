# Example backend configuration for local development
# Copy this file to backend-local.tf and update with your actual values

terraform {
  backend "s3" {
    region         = "us-east-1"
    bucket         = "my-terraform-state-kafka-eks-12345"
    key            = "kafka-eks/terraform.tfstate"
    dynamodb_table = "terraform-locks"
  }
}